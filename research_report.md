# 연구 보고서 초안

> 상태: 학습(CPCV·OOS) 미완료. 설계와 파이프라인 중심으로 정리한 초안.

---

## 1. 초록

바이낸스 BTCUSDT aggTrades(틱)를 달러 바로 자른 뒤, 그 바의 달러 불균형 방향으로 베팅하고 트리플 베리어 메타 라벨을 붙이는 매매 시스템을 설계했다.

바를 자르는 기준은 어제 UTC일 거래대금의 1/650인 daily_T$이며, 이 값을 [0.5D, 2D] 구간으로 클립한 뒤 EWMA로 갱신한다. 바가 닫히면 Primary는 그 바의 signed_flow(θ) 부호를 그대로 매매 방향으로 사용한다. Meta는 flow_strength(|θ|/바 대금)와 sigma를 피처로 받아, 익절 장벽에 먼저 닿을 확률을 예측한다.

이벤트는 모든 달러 바 종가(every_bar)를 사용하며, CUSUM 필터는 최종 파이프라인에서 제외했다. 학습은 IS 구간에만 수행하고 CPCV(C(6,2)=15)로 검증하며, OOS는 모델 확정 전까지 사용하지 않는다.

현재는 라벨·피처·CPCV 구조까지 구현된 상태이며, 본격적인 모델 학습과 OOS 평가는 진행 중이다.

---

## 2. 파이프라인이 현재에 이르게 된 이유

초기에는 달러 불균형 바로 자른 뒤, 바 종가의 로그 수익률에 CUSUM 필터를 적용해 이벤트를 선별했다. 불균형이 쌓인 방향과 가격 추세(CUSUM)가 같은 쪽일 때만 진입하는 방식이었다.

그러나 실제로 보면, 달러 불균형이 이미 강하게 나타난 바에서 CUSUM이 추가로 걸러내는 효과가 거의 없었다. 불균형이 심한 구간은 이미 가격이 한쪽으로 치우쳐 있는 경우가 많았고, CUSUM은 그 정보를 한 번 더 확인하는 수준에 그쳤다. 오히려 이벤트 수를 줄여 Primary의 recall을 떨어뜨리는 부작용이 더 컸다.

그래서 CUSUM을 메인 경로에서 빼고, 달러 바가 닫힐 때마다 이벤트로 삼는 every_bar 방식으로 바꿨다.  
바를 자르는 기준(거래대금)과 방향을 정하는 기준(θ의 부호)을 명확히 분리한 것이 현재 파이프라인의 핵심이다.

---

## 3. 하이퍼파라미터·피처·모델 선정 이유

### 바 생성 관련

- **달러 바를 선택한 이유**: 시간 바는 거래 활동을 제대로 반영하지 못하기 때문이다. 거래대금이 일정 수준에 도달했을 때 바를 닫는 방식이 시장 흐름을 더 잘 나타낸다.
- **daily_T$ = 어제 대금 / 650**: 후보 divisor(100~1000)를 같은 틱으로 비교한 결과, 650이 바 개수·세기·길이의 균형이 가장 나았다. 더 빨리 반응하게 하려고 400보다 650을 택했다.
- **T$를 [0.5D, 2D]로 클립**: 오늘 거래대금이 어제보다 클 때 바가 더 많이 나오게 하기 위해서다. 클립을 빼면 T$가 당일 대금에 따라가서 하루 바 개수가 비슷해지는 문제가 생긴다.
- **max_ticks, init_T**: 바가 비정상적으로 길어지거나 첫 바가 동떨어지지 않게 하는 장치다. 숫자는 아직 임시값이다.

### Primary

- **sign(θ)만 사용**: 그 바의 달러 불균형 방향을 그대로 따른다. 약한 불균형도 버리지 않아서 Primary의 recall을 최대한 높인다. 정밀도는 Meta가 담당한다.
- **every_bar**: 바를 미리 걸러내지 않는다. CUSUM을 썼을 때처럼 이벤트를 줄이면 recall이 떨어지기 때문이다.

### Meta 피처

- **flow_strength = |θ| / 바 대금**: 달러 바로 잘랐기 때문에, 세기 또한 그 바의 대금으로 나누는 것이 자연스럽다. E[θ]로 나누는 방식은 대조군에서만 사용한다.
- **sigma**: 트리플 베리어 폭을 정하는 데 쓰는 변동성이다. 피처로도 같이 넣었다. 값은 아직 임시다.

### 라벨

- **y=1**은 “서로 다른 바에서 익절만 먼저 닿은 경우”만 인정한다. 같은 바 안에서 익절·손절이 동시에 발생한 경우는 순서를 알 수 없으므로 0으로 둔다.

### 검증·모델

- **CPCV (6그룹 중 2개를 테스트로 쓰는 15경로)**: AFML에서 권장하는 방식이다. 단순 연도 hold-out보다 시간 구조를 더 잘 반영한다.
- **Purge + Embargo = 1τ**: 학습 샘플과 테스트 샘플이 겹치지 않게 하기 위한 최소 장치다. 길이는 아직 임시값이다.
- **채점 지표는 로그로스**: 메타가 낸 익절 확률이 실제와 얼마나 맞는지 보는 데 적합하다.
- **모델은 Random Forest**: 구현이 간단하고, 피처 중요도를 바로 볼 수 있어서 초기 모델로 선택했다. 트리 개수·깊이 등은 아직 임시값이며 CPCV로 나중에 조정할 예정이다.

---

## 참고

- 본 문서는 README.md의 [선정] / [임시값] 구분과 파이프라인 설명을 바탕으로 작성했다.
- 학습이 완료되면 결과 섹션을 추가할 예정이다.
